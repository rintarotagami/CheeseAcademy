name: sakura

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - name: FTP Deploy
        uses:  SamKirkland/FTP-Deploy-Action@v4.3.5

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.ftp_password }}
          local-dir: ./Users\coneko\Desktop\VScode\G'sAcademy\CheeseAcademy
          server-dir: /home/rin-study/www/cheese-academy.com/

